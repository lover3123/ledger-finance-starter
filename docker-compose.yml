services:
  mongo:
    image: mongo:7
    container_name: ledger-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - ledger_mongo_data:/data/db
volumes:
  ledger_mongo_data:
